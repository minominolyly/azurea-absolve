const INDEX: string = "/";
const RULE: string = "/rule/";
const QUIZ: string = "/quiz/";
const GALLERY: string = "/gallery/"

const RoutePath = {
  INDEX,
  RULE,
  QUIZ,
  GALLERY,
};

export default RoutePath;
